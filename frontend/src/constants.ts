// ── Shared constants (single source of truth for frontend + backend alignment) ──

export const MAX_NAME_LENGTH = 100;
export const MAX_QUANTITY = 9999;
export const SEARCH_DEBOUNCE_MS = 300;
export const LINK_SEARCH_DEBOUNCE_MS = 200;
export const SCANNER_DUPLICATE_COOLDOWN_MS = 1500;
export const SCANNER_PROCESSING_TIMEOUT_MS = 5000;
export const FEEDBACK_DISPLAY_MS = 1500;
export const OFFLINE_FAILURE_THRESHOLD = 2;